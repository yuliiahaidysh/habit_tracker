export const TOAST_TIMEOUT = 5000;
export const ERROR_BANNER_TIMEOUT = 6000;

export const FORM_CONSTRAINTS = {
  MAX_NAME_LENGTH: 100,
  MAX_DESCRIPTION_LENGTH: 500,
} as const;

export const WEBSOCKET_CONFIG = {
  PORT: 4000,
} as const;

export const MILESTONES = {
  ONE: 3,
  TWO: 7,
  THREE: 30,
} as const;
